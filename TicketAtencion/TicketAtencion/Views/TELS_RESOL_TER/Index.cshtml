@model IEnumerable<Bdd.TELS_RESOL_TER>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TEL_RESOL_TER)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RESOLUTORES_TERCEROS.NOM_RESOL_TER)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TEL_RESOL_TER)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RESOLUTORES_TERCEROS.NOM_RESOL_TER)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TEL_RESTER }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TEL_RESTER }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TEL_RESTER })
        </td>
    </tr>
}

</table>
