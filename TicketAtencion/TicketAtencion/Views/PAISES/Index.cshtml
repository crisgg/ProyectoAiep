@model IEnumerable<Bdd.PAISES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOM_PAIS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MONEDA.MONEDA1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOM_PAIS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MONEDA.MONEDA1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PAIS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PAIS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PAIS })
        </td>
    </tr>
}

</table>
