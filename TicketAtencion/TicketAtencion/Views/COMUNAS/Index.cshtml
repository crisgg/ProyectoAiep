@model IEnumerable<Bdd.COMUNAS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOM_COMUNA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DISTRITOS.NOM_DISTRITO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOM_COMUNA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DISTRITOS.NOM_DISTRITO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_COMUNA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_COMUNA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_COMUNA })
        </td>
    </tr>
}

</table>
