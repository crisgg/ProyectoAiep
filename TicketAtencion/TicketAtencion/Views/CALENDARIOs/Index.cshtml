@model IEnumerable<Bdd.CALENDARIO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_NO_LABORAL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION_NO_LABORAL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PAISES.NOM_PAIS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_NO_LABORAL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION_NO_LABORAL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PAISES.NOM_PAIS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_FERIADO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_FERIADO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_FERIADO })
        </td>
    </tr>
}

</table>
