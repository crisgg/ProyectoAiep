@model IEnumerable<Bdd.DATOS_EXTRAS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AGENTES.RUT_EMPRESA_PROV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AGENTES.RUT_EMPRESA_PROV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_DATOS_EXTRAS }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_DATOS_EXTRAS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_DATOS_EXTRAS })
        </td>
    </tr>
}

</table>
