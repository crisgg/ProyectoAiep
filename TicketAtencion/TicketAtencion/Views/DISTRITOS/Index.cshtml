@model IEnumerable<Bdd.DISTRITOS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NOM_DISTRITO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.REGIONES.NOMBRE_REGION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NOM_DISTRITO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.REGIONES.NOMBRE_REGION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_DISTRIRO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_DISTRIRO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_DISTRIRO })
        </td>
    </tr>
}

</table>
